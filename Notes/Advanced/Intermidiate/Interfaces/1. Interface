1. An interface can consist of only incomplete method in it,
if complete method are developed it will give error.

interface to class -----> implements
class to class -----> extends
interface to interface ------> extends

package p1:
public interface A{
    public void test(){ // error

    }
}