Scriplet Tag -- <% %>

Inside scriplet tag we can write partial java code but we can't use access specifier(Public, Protected, Private)
and it support built in implicit object so we 
and we can't create method inside of it.


2. Declaration Tag --<*! %>
we can declare method,
we can us access specifier


we can't use implicit object 
we can't write html code inside of it.

3. Expression Tag <%= %>
We can't use semicolon and 
we can't write more than one statement

4. Directive Tag
    1. Page Directive tag
    2. Include Directive Tag

it helps to import file and package @


